Tag_Info {
	types: Array = []

	@operator == @infix 500 { left, right;
		left.types == right.types
	}

	@operator + @infix 700 { left, right;
		info := Tag_Info()
		info.types = left.types + right.types
		info
	}

	include? { type;
		types.include?(type)
	}
}
