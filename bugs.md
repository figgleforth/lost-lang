+ `@puts [Number, String]` (an Array of two or more bare Types) raises `Cannot_Call_Instance_Member_On_Type` at `lost/array.tape:117` (`to_s`'s `it === String` check). A single bare Type in an array prints fine; found incidentally while verifying Enum docs.
+ `@declare structure.columns` had no effect when called in new{;}
+ Dict literal `{type: Task_Type.TODO}` mis-parses: the general identifier parser's `: Type` lookahead greedily claims `type: Task_Type` as a typed identifier instead of a dict pair, leaving `.TODO` dangling, raising `Invalid_Dictionary_Infix_Operator`. Repro: `{type: Task_Type.TODO, done: false}`.
+ `for` loop reuses one Scope object across all iterations (mutates `it`/`at` in place rather than allocating fresh each time, unlike `interp_each_loop`'s `.each`/`.map`-style path), so a closure built inside a `for` loop body captures that shared, mutating scope and sees the final iteration's value once called later, not its own. Repro: `stmts := [], for [1,2,3]; stmts.push(\`it\`); end, for stmts; @puts it(); end` prints `3, 3, 3` instead of `1, 2, 3`. Same bug class as `var` in a pre-`let` JS loop. Found via temp/scope_visibility_brainstorm.txt.
+ String literals interpret to a raw Ruby `::String`, not a wrapped `Lost::String`; so per-literal metadata (e.g. `quotation_style`, added for Member/Struct display) only survives when a value is used directly at the same call site; it's gone the moment the string passes through a variable first. Want to make string literals interpret to a real `Lost::String` instance generally. Bigger than it sounds: touches every place that currently assumes a raw Ruby string (native string methods, `.is_a?(::String)` checks, Ruby proxy methods). (The raw-string-on-the-left `==` asymmetry this used to also list is fixed now: `Lost::String#to_str` closes it, see Done.)
+ `src/compiler/parser.rb`: `begin_expression`'s bare-comma branch (`eat and nil`) isn't dead/mistake-only code. Tried raising there and it broke `Tuple { values, ... }` in `preload.tape` itself. The nil-init idiom (`ident,`) only parses the identifier, not its trailing comma (`parse_nil_init_expr` never eats it), so the *next* loop iteration of whatever's collecting body/list expressions (`parse_type_decl`'s member loop, at least) lands on that leftover comma and relies on this branch to silently swallow it. A real fix needs to tell "leftover separator from a nil-init decl" apart from "genuinely stray/doubled comma", not obvious how to do that from `begin_expression` alone, since by then the caller's context (what loop we're inside, whether the prior expression was nil-init) isn't visible. Punting on "make bare comma-sequences imply a Tuple" entirely too: top-level `a, b` already means two statements throughout the language (`funk(true), funk(false)` in test/regression_test.rb), so that'd be a breaking semantic change, not a bug fix.
+ Bare Type stringification is unreliable: `Number.to_s()` "works" by accident (a Ruby proxy quirk echoes the type's own name back), but Lost-level `to_s{;}` (String, Array, ...) reads an instance member that's still `nil` on a bare, never-constructed Type, so it prints empty. Found via `<String, Number>.to_s()` showing `<, Number>`. No real "what should a bare Type print as" mechanism exists yet.
+ Parser hangs forever (infinite loop, not an error) on `foo(X < Y)`, a capitalized-identifier comparison used as a call argument. Confirmed pre-existing on `main`, unrelated to the `TYPE_IDENTIFIER '<'` dispatch fix above (same hang with and without it); something in `parse_circumfix_expr`'s argument loop specifically, not the top-level statement case that fix covers. Not chased down; do not add as a test without a way to bound it (would freeze the whole suite).
- `src/compiler/type_checker.rb`: `type_by_identifier`/`func_signatures_by_identifier` were flat hashes keyed by bare name, scope-blind: an unrelated same-named local in one function poisoned inference in another (`f1 { x: Number = 5 }` falsely broke an unrelated `add(x)` call in `f2 { x := "hello" }`). Fixed with a real scope stack, searched innermost-first like the interpreter's own identifier resolution.
- A trailing `#` comment on the last line of a function or program body silently becomes the return value instead of being discarded. Repro: `add { a, b; a + b  # sum them }` then `add(4, 8)` returns the comment `"sum them"` instead of `12`
- `Array#map` crashes if the anonymous function doesn't explicitly declare `it`/`at` as params (`arr.map({; it * 2 })` fails; `arr.map({it, at; it * 2})` works): documented inline in `lost/array.tape`.
- Nil-safe access: `x.?method` should return nil instead of raising when `x` is nil.
- `=~`/`!~` (regex match) are listed in `COMPARISON_OPERATORS` (`src/shared/constants.rb`) but missing from `INFIX`, so the parser never builds an `Infix_Expr` for them: same bug class the `!==` fix addressed. `'abc' =~ 'xyz'` silently parses as disconnected expressions instead of erroring. Needs `=~`/`!~` added to `INFIX`, and an actual regex-match implementation.
- Route registration bug found while investigating the struct-variant redesign below: a named-handler route (`get://path some_handler`) was keyed in the route table by the handler function's *name* instead of `method:path`, so two different routes sharing one handler silently collided and the earlier one was dropped. Always keyed by `method:path` now.
- Bare annotated properties (`x: Number`) only self-declared on the Type at declaration time, not per-instance, so assigning to one inside `new` before ever reading it first raised `Cannot_Assign_Undeclared_Identifier`. Now self-declares eagerly per-instance, matching how the comma idiom (`x,`) already worked.
- `Array#include?` was a raw `@ruby` proxy, so it compared elements with Ruby's native `==` and never saw an Lost-level `@operator ==` overload on a custom Instance (e.g. two structurally-equal `Member`s compared unequal). Converted to an Lost implementation (same pattern as `find`/`any?`/`all?`), so it goes through the interpreter's own `==` dispatch.
- Bare `return` (no value) raised `Interpret_Expr_Not_Implemented` instead of yielding `nil`.
- `.?` only swallowed a missing member on plain Scope receivers: `[].?x`/`{}.?x`/`(1..5).?x` still raised, and `Array.?uniq` (instance method looked up on the bare Type) raised `Cannot_Call_Instance_Member_On_Type` unswallowed. Now covers all receiver kinds and both error types.
- `interp_dot_range` silently returned the range itself for any `.member` besides `.each` instead of raising. Now routes through the same `interp_dot_scope` path Array/Tuple use; `Lost::Range` gained a `has?` stub so that path doesn't crash with `NoMethodError`.
- Structured-type-variant storage redesigned: was a single mangled string key (`"String<Dictionary>"`, types only, never names), two declarations differing only by slot name silently collided/merged into one variant. Now a per-scope list (`Scope#structured_type_variants`), matched by real structure equality instead of a string.
- Fixed the "lone unnamed Struct value spreads" rule firing at reference sites too, not just declarations: `Table_With_Opts<Options>` (bare reference) used to explode `Options` into its own fields instead of treating it as one opaque value, so it could never match a named-slot declaration. References now never spread; only declarations do.
- Be able to instantiate Structs: `<name: String, age: Number>('Alice', 30)` now works. `<...>()` (calling a bare struct literal) was completely broken before (`interp_struct_call` referenced undefined locals, always raised `NameError`), now builds a real instance with the call's arguments as per-slot values.
- `Array#==` was Ruby-native (`values == other&.values`), so it never saw an Lost-level `@operator ==` on element Instances. Added a real `@operator ==` to `lost/array.tape`. Separately: `!=` never derived from a declared `==` anywhere in the language: any type declaring `==` but not `!=` silently fell back to Ruby identity for `!=`. Fixed generally: `!=` now derives from a declared `==` (negated) when it has no overload of its own.
- Added `to_s{;}` to `Array`, `String`, `Dictionary`, `Bool`, `Nil`, `Tuple`. Surfaced a chain of real, previously-hidden bugs along the way: Dictionary/Tuple literals never linked `enclosing_scope` at all; Tuple's own construction passed the type-name string `'Tuple'` as its `values` constructor arg by mistake, and `tuple.values = values` only reassigned the Ruby attr, never `@declarations['values']`, so `t.values` on a real Tuple returned the stale string `"Tuple"` forever; `nil`/`true`/`false` were never linked to their declared types either; `Lost::Nil` was `< Scope` instead of `< Instance`; and `Lost::Bool::TRUE`/`FALSE` were defined *above* `def initialize` in the same class body, so Ruby silently used the wrong inherited initializer, and `truthiness` was never actually set for either singleton.
- `parse_type_decl`'s composition-chain loop (`until curr? '{'`) had no exit besides finding one: any composition typed with no `{ }` body (`A & B`, any operator) hung the parser completely, forever. Turns out that's a legitimate pattern, not an error (see design.md for the resulting feature).
- Removed `Lost::Shape`'s `:defaults` hash. Fixed a clobbering bug this surfaced: `build_shape` only re-asserted `.fields` after the declared `Shape` type's own body ran (clobbering the rest back to empty), now re-asserts all four.
- `.shape` on a tagged type reference (`Abc<value>`) bypassed `#build_shape`, so `.shape.fields` came back empty. Now goes through `#build_shape` like every other shape construction.
- `#build_shape`'s Field loop skipped unnamed fields, dropping them from `.fields` and desyncing later indices from `names`/`types`/`values`. Every field slot gets a `Field` now (unnamed = nil name).
- Parser: standalone `<...>` shape literal failed to parse unless its first field looked like a type name (`<1+2+3/123>` broke, `Abc<1+2+3/123>` didn't). Fixed by treating any leading `<` in expression-start position as a shape literal unless a custom prefix operator claims it. Noted, not fixed: custom prefix operators (`<`, `~neg`) were already broken pre-existing on `main`.
- Circumfix groupings (`()`, `[]`, call args) had the same bare-identifier-before-`,` ambiguity `<...>` was already fixed for: `foo(missing, 5)` silently turned `missing` into `nil` instead of raising `Undeclared_Identifier`. Fixed the same way.
- Fixed Field/Shape display: an unnamed field's `type` slot held its own raw value (Field showed `<1>` as `"1"` instead of `"Number"`), and a nil field name printed a stray `"nil: "` prefix. Surfaced a pre-existing, unrelated bug along the way: `Lost::String` never overrode `==`, so two same-content instances (or a wrapped value vs. a raw literal) were never equal, fixed with real content comparison on `Lost::String` itself.
- Two bugs surfaced while adding `learn/*.tape` docs: (1) postfix `unless`/`until` (`expr unless cond`, `expr until cond`) put the body in `when_false` instead of `when_true` like the block form does; ran `unless` backwards and made `until` hang forever (empty loop body). (2) The parser's prefix-operator check matched by value only (needed for `return`/`not`, lexed as plain identifiers) with no type guard, so a string literal whose content collided with a prefix symbol (`'!'`, `'-'`, `'return'`) got misparsed as that operator instead of the string value: `"hi!".end_with?('!')` raised `Interpret_Expr_Not_Implemented`.
- Closed the remaining `Lost::String#==` asymmetry: a raw Ruby string literal on the *left* (`message == File_System.read(...)`, where the read comes back wrapped) still hit Ruby's own native `String#==` instead of `Lost::String#==`. Added `#to_str`: Ruby's own `String#==` already delegates to `object == self` for a right-hand object that responds to `to_str`, landing back on `Lost::String#==` correctly either direction.
- `refute_raises` called with no explicit exception class: `rescue *[] => e` catches nothing at all (not the same as a bare `rescue`), silently a no-op everywhere it was already used that way. Surfaced a real, pre-existing bug: `#find_operator_overload` treated a bare Type object the same as an instance (`operand.is_a?(Lost::Scope)`, not `Lost::Instance`), so comparing two Type objects directly wrongly dispatched into the type's *instance*-level `==` overload.
- Dot-infix double-interpretation: `interp_dot_infix` interpreted `expr.left`, then the Array/Range/Dictionary/scope sub-handlers each re-interpreted it from scratch (up to 3x), so a receiver with side effects ran multiple times. Sub-handlers now take the already-interpreted receiver.
- Operator-overload dispatch double-evaluated operands: `interp_infix` interpreted them to find the overload, then handed the raw expressions to `interp_func_body`, which interpreted them again. Fixed at all three sites (arithmetic, comparison fallback, custom-operator) via `#interp_func_body`'s new `arg_values:` param.
- Bare `X.new` was a drifted partial copy of `X()`: `interp_dot_new` built the instance by hand (no Ruby-class backing, no `enclosing_scope` link, no `.structure` binding) and skipped `new{;}` entirely. Now delegates to `interp_type_call` (shared construction in `#build_instance_of_type`), so it's fully equivalent to `X()`.
- `interp_for_loop`'s `Lost::Struct` branch called `types_by_field_name`, a method that never existed: `for` over a Struct always raised `NoMethodError`. Now iterates `.members`.
- A struct reference member named via `:=` (`Table<columns := cols, database := db>`) bound the member's own *resolved type* onto `.structure`, not the real value: `interp_type` read `supplied.type_objects` where it should've read `supplied.values`.
- A lowercase value right after `:` in a struct member (`<columns: cols>`) used to parse silently: struct members only have `name: Type`/`name := value`, so `#parse_identifier_expr`'s `: Type` lookahead declined the `:` without consuming it, and the next loop iteration reparsed the leftover `: cols` as an unrelated `:symbol` prefix literal. `#parse_struct` now raises `Lost::Invalid_Struct_Member_Annotation`.
- `123.numerator` returned `nil` instead of `123`: `maybe_instance` wrote straight to `Lost::Number`'s Ruby attrs, never syncing `@declarations`. Gave `Lost::Number` a real `initialize` that syncs both.
- `../test := 123` could self-declare a brand-new static from any method, even called long after the type was defined. Added `Type#declaration_in_progress` (set during `#finish_type_declaration`'s body walk), gating `../` self-declaration the same way `./` is already gated on `has?('new')` for instances.
- Strings never printed with quotation marks anywhere. Added `String#pretty_print{;}` (quoted, honors `quotation_style`) kept separate from `to_s` (must stay unquoted: it's the interpolation-based string-building primitive; quoting inside it recursed infinitely).
- `begin_expression`'s struct-literal check (`curr?('<')`) matched by lexeme value only, so a string literal `'<'` was misparsed as a struct-open and swallowed the rest of the file. Added a `curr_lexeme.type == :operator` guard. Found via `examples/aoc/2015/03/part1.tape`'s `elif it == '<'`.
- `elsif`/`elswhile` were reserved in `constants.rb` but never wired up; only `elif`/`elwhile` actually worked. Both now parse and interpret identically to their counterparts.
- `test/advent_of_code.rb` wasn't matched by the Rakefile's `test/**/*_test.rb` glob, so it silently never ran under `rake test`. Renamed to `advent_of_code_test.rb` (class `Advent_Of_Code_Test`), which surfaced the `<`-struct bug above and the compound-assignment bug below, both previously undetected.
- `instance.member += value` silently no-op'd: `interp_compound_infix` resolved its assignment target via `scope_for_identifier`, which only understands plain identifiers, so a dot-target fell through to `stack.last` and declared a bogus `nil`-named identifier instead of touching the member. Now routes through `assign_dot_member`.
- Tuple/Array dot-index access (`.N`/`.N.M...`) silently returned nil past the collection's length, and silently truncated a non-integer index. `.0.1` lexes as the single float `0.1`, which Ruby's own `Array#[]` truncates to index 0, so `((), true).0.1`/`.0.2`/`.0.3`... all silently returned the same first element instead of erroring. New `#array_index_value` helper + `Lost::Invalid_Array_Index`.
- `<=>` on a custom Instance with no `@operator` overload silently returned `nil` instead of raising. Ruby's `Kernel` gives every `Object` a trivial, identity-based `<=>` default, so `respond_to?(:<=>)` alone couldn't tell that apart from a real one. Now also checks the resolved method's owner isn't `Kernel`.
- Calling a non-callable value (`dict := {keys: 4}, dict.keys()`) now raises `Cannot_Call_Value` instead of the misleading `Cannot_Initialize_Non_Type_Identifier`.
- String interpolation couldn't see `@operator` declarations registered elsewhere in the program: `interp_string` now reuses the interpreter's own `@parser` instead of a fresh, isolated one.
- `for x by n` stride chunks were raw Ruby Arrays, not `Lost::Array`: broke `==`/`.push` on `it`. Same bug found across several more proxies: `Array#first`/`#last`/`#slice`/`#reverse`/`#sort`/`#uniq`, `String#split`/`#chars`, `Dictionary#keys`/`#values`/`#merge` all returned raw Ruby Array/Hash instead of wrapping the result. All now wrap.
- `Member#to_s` crashed displaying an unnamed type-only struct member (`<String, Number>`'s String slot, `value` = the bare Type itself, not an instance), fixed with `.?pretty_print()`.
- `X < Y` (capitalized comparison) raised `Out_Of_Tokens`: `TYPE_IDENTIFIER '<'` always committed to `parse_type_decl`/`parse_struct`. New `#try_parse_type_decl` actually attempts the real parse and rewinds the token position on any syntax error, rather than guessing via lookahead.
- `f { ABC; ABC }` crashed with a raw `NoMethodError` (`param.name.value` on nil): a bare Capitalized/UPPERCASE param parses as a signature-literal's bare type (`{Number -> String;}`), not a name, and `#interp_func_body` assumed every param had one. New `Lost::Invalid_Parameter_Name`.
- The stdlib scope had no strong reference anywhere, only the readable-scope WeakMap entry, which never keeps anything alive by design. One real GC pass and the whole stdlib (String, Array, ...) could vanish mid-program. Anchored with a real `@stdlib_scope` instance var.
- Static declared via `Self.x := value` was silently reset on every construction (its AST shape wasn't recognized by the per-instance re-run skip-check), and `self.some_private_member` wrongly raised `Cannot_Call_Private_Instance_Member` from inside its own instance's method (the privacy check compared against `stack.last`, which is never the instance itself during a method call). Both found and fixed while adding `self`/`Self` keywords.
- `Self.database := ~/db` in `lost/table.tape` raised `Cannot_Call_Instance_Member_On_Type`: `self`/`Self` dot-writes on an already-existing member fell through to the stricter external-`.`-write rules (permission checks `./`/`../` never ran). Fixed by routing self/Self writes around those checks entirely, for both new and existing members.
- Two pairs of duplicate-named test methods (`test_dot_slash_regression`, `test_identifier_lookup_regression`) silently shadowed each other in `test/regression_test.rb`; Ruby just keeps the later definition, so the first of each pair never actually ran. Fixed the `./`-related one (its stale assertion was wrong); the other left as a follow-up.
- `Type<Struct> {}` never bound its own bare name like a bare `Type {}` does, so `Task()`/`Task.method()` raised `Undeclared_Identifier` right after `Task<Schema> | Table {}`. Now resolves when exactly one variant exists (mirrors Bare Named Structs); still unreachable if genuinely ambiguous.
- Return-type contracts (`-> Table`) used exact-name equality, wrongly rejecting a composed return value (`Task | Table {}` returned where `-> Table` was declared). Now checks the full composed-type set.
- `database.rb`'s `.to_sym` calls assumed raw Ruby strings; broke on `Lost::String` (e.g. passed via `structure.name`). Fixed via `.to_str.to_sym`.
- `proxy_create_table`'s primary-key branch used `type_name.to_sym` instead of `member.name.to_sym`, hardcoding every primary-key column's name to `"Primary_Key"`.
- `Member#to_s` treated a real `false` value as "no value" (`if value`), so `done := false` displayed as `done: Bool` with no value.
- `!=` deriving from a declared `==` crashed against an incompatible right operand: `lost/string.tape`/`member.tape`/`struct.tape`'s `==` all assumed `right` was already the same type. Fixed with `.?` safe navigation.
- `struct.tape`/`member.tape`'s own `@operator === @infix` could never run -- `===` is a built-in the interpreter always resolves first. Confirmed dead, both removed.
- `Any`'s wildcard treatment initially skipped `=>=`/`=<=`/`=/=`: `X === Any` was `true` but `X =>= Any` stayed `false`, contradicting "`===` is mutual `=>=`". All seven now share the same check.
- `Table#create`/`#update` handed values straight to Sequel, which can't literalize an `Lost::String` or a raw Symbol (treated as a column reference, not a string). New `#sql_literal_value` unwraps both.
- `build_instance_of_type` only set `.name`/`.types` via Ruby attrs, not `@declarations`; a composed type sharing a built-in's Ruby class (`Tasks | Table {}` -> `Lost::Table`) had `instance.name` stuck on the backing class's own name ("Table").
- Same bug in `#build_enum`: `Lost::Enum.new` (no name arg) baked "Instance" into `@declarations['name']`; a struct member typed with a user enum always displayed "Instance" instead of the real name.
- Lexer: `>` immediately followed by `,`/`;` (no space) lexed as one bogus operator token instead of two, breaking struct annotations followed by either. Added both to `ILLEGAL_OPERATOR_CHARS`.
- Re-declaring a bare named struct with the identical shape raised `Undeclared_Type_Structure` instead of being a no-op. Now allowed. Also made `Struct#structure_declaration_equal?` order-insensitive once every member is named (`<a: X, b: Y>` == `<b: Y, a: X>`); still positional when any member is unnamed.
- `Undeclared_Type_Structure`'s own error message crashed rendering a named member with no `: Type` annotation (`done := false`, `.type` nil). Falls back to rendering the default value.
