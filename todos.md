+ Finalize Enums (`::`): type annotations (`Task_Type :: Number { ... }`, per-member `: Type`) are parsed and stored (`.type`/`.types`) but never enforced anywhere -- no `Type_Contract_Violation` on a mismatch. Decide whether/how to enforce before relying on enums for anything type-sensitive; not documented as a real feature yet, see readme.md/CLAUDE.md notes.
+ Reserch whether FFI with systems languages like C, C++ possible? I think yes.
+ Be able to compose with parts of a class. Like `|Thing.all_functions`, `&Thing.specific_function`, `.variables`, `.constants`, etc.
+ A few of the Advent of Code tests are super slow because they crunch a lot of data. One of them was like 50% of the entire testing time. I don't want to leave them completely untested. Look into maybe some env var that tracks number of times tests have run, then every n full test runs (maybe 100) then test those as well.
+ Make all directives passthrough (return the expression given) or at least as many it makes sense for
+ `@connect <Database>` should return `(db: Database, ok: Bool)`. And then add `Database.connect{-><Database, Bool>;}` to hide the directive. It'll be easier for the user, not having to remember @ vs not.
+ Untested stdlib methods found via audit (2026-08-16): `response.redirect`, `Statement(other)` construction path, `Struct#has?`, `Array#prepend`, `Database#delete_table`, `String#to_md5_hash`, `Server#ok200`
+ Current limitation: `@push_scope`/`@pop_scope` only accept a bare identifier naming something already bound (a Type name or a variable); a literal or a constructor call (`Number(4)`) is rejected, since it builds a fresh object on every evaluation and can never satisfy the identity check on pop. Figure out how to make instances work too, not just Types/existing variables (numbers, strings, etc.).
+ Add classic for-loop `for i := 0, i < 10, i++`
+ `test/interpreter_test.rb` (3251 lines, biggest test file) is organized chronologically, not by topic. Split it into `functions_test.rb`, `privacy_test.rb`, `loops_test.rb`, `collections_test.rb`, `operators_test.rb`, `unpack_test.rb`, `declarations_test.rb`, `statements_test.rb`, `file_loading_test.rb`; fold type-decl/composition tests into existing `composition_test.rb`, maybe fold routes/html-fence into `server_test.rb`. Maybe do this opportunistically, not as a standalone refactor.
+ Ask Claude to come up with with tangible examples of how various custom operator precedences would behave in IRL examples with standard operators. Just something I can see and visually understand.
+ Multiple return values: `{-> Abc, Def; }` or named `{-> a: Abc, b: Def; }` or named with defaults `{-> a: Int = 1; }`. Doesn't change anything for caller, but lets you define default output and a variable to use in the body. I suppose if you return a tuple of them like `f {-> a := 1, b := 2; }`, you can expand or whatever I called it. `a, b := f()`. And notice you don't need to have a body when there are defaults. I guess even without a default, things can be nil'd or something.
+ Add `%hash()` that converts expressions to string, then hashes the string.
+ Add `%dict()` that converts expressions to string, then creates a dictionary with those keys and nil values
+ Add `Statement.new{Statement;}` so you can create statements programmatically as well as with the backtick literal.
+ Make `Fence_Expr` extend `String_Expr`? Or `Statement_Expr`? Both make sense, it's basically a multiline string, that can have a header, like ```md\n``` or css, html, etc
+ Every value passed around inside of Ore should be an Ore::Class of some kind. calling `#maybe_instance` in `#interp_circumfix` for starters.
+ A `Command_Line` class. Lets you execute commands from Ore::String and Ore::Fence. Inspired by Tsoding and Jai.
+ The way Ore::Classes are created differs in many places. I should have some kind of `Interpreter#make klass` that does all the linking to the runtime and stuff
+ Add runtime types for all instantiable constructs in the language
+ Switch statement / pattern matching: required for `@help` / `help{expr;}` function.
+ Rename preload.ore to global_scope.ore. That's what the global scope will be loaded from, gives me better control. Would be cool to construct different scopes that users can pick to be their global scope, or make their own.
+ Constants should be able to be declared with a type and without a value. The first assignment locks in the value. `VERSION: Number` and assign it once later.
+ Rewrite the `Dom_Renderer` in Ore, and remove the magic rendering whenever a Dom is returned. Make the user render themselves, but implement it for them. Everyone prefers control over magic probably.
+ Table associations (`belongs_to`/`has_many`)
+ Add jsonb-like column support to databases.
+ No JSON encode/decode exposed to Ore: `require 'json'` only used internally, for example when parsing POST bodies (`interpreter.rb`).
+ No outbound HTTP client: Ore can serve requests but can't make them.
+ No `ENV`/config access: only I/O primitive is `File_System`.
+ No in-Ore testing facility: Minitest only tests the interpreter itself. Ore has an `@assert` command taking condition and message.
+ Stride overlap for `for x by n,overlap`: `getting_started.md` documents `for x reject by 2,1` / `for x each by 3,1` style overlapping chunks, but the parser doesn't support the second stride argument yet (`src/compiler/parser.rb`: "Currently `stride` doesn't support option to overlap elements"). This was meant to be implemented, not just aspirational docs: needs the parser to accept `by <stride>,<overlap>` and the interpreter's chunking (`each_slice` today) to respect the overlap instead of using non-overlapping slices.
+ `help {expr;}` or `@help` that accepts any expression and returns information about the Type, primitive, function, etc. Literally any expression should be able to be described given its AST. You even have access to the live values so the information can be dynamic to reflect what the user is actually asking about.
+ Improve error messaging. `src/runtime/error_formatter.rb` doesn't display source code properly for some expressions. They have to override `#detail_message`.
+ Some kind of `to_s;` and `inspect;` equivalents for printing objects
+ A way to get capitalization at runtime `:Identifier, :identifier, :IDENTIFIER` from any given identifier.
+ Ability to coerce types when declaring http routes. `put://survivors/:name { name: String; }`
+ Currently `func{a;}` must be called with parens `func(a)` otherwise `func` evaluates to a pointer to this func. I want to borrow from Jai: `x := func.*` should give you the pointer, `func` and `func()` should be a calls as usual, and then somehow I need to parse arguments without parens.
+ Add another version of `@puts` that prints the expression as a string. Like `@puts! "`(1+2)`"` should print `(1+2) => 3`
+ Add a builtin debugger, `@debug` maybe. It should stop execution and let you step through, and treat the current place as a repl that you can do whatever in. My hunch is a separate Interpreter#output, like #debug_output that is capable of interrupting the stream of expressions being interpreted. `@debug when_condition_true` would be neat too.
+ When the only argument to a function call is a function like `map({it; ...})`, let user just splat it like `map(it; ...)` and I even like how Swift (I think) let's you pass it as a trailing block after the call itself like `map() {it; ... }`.
- Validate whether the precedence is even used when declaring an operator. Yes, it is.
- `src/runtime/interpreter.rb`: array `<<` is special-cased in the interpreter instead of being a real operator declaration on `Array`; revisit once operator declarations exist.
- Add Type set comparison operators
- Function signature literals (`Type{Param;}` as a value/alias, `name: Type{Param;}` self-declaring): `Ore::Func_Signature`, with `Invalid_Func_Signature` raised for malformed ones.
- Return-type annotations on real functions: `name: Type {}` prefix and `{params -> Type; ...}` inline (works for anonymous functions too): plus runtime enforcement (`Type_Contract_Violation` if the actual return value doesn't match).
- Structural signature matching: reassigning a signature-aliased identifier now checks a real function's actual param/return types against the alias, not just nominal string equality.
- Replace all `# todo: Proper error` placeholders scattered through `src/runtime/interpreter.rb`: real error types needed instead of generic ones. And add tests. A few of the errors are unreachable because of other guards, but the ones that are have a test now.
- `src/compiler/lexer.rb`: Add operator symbol exclusion list `' " { } ( ) [ ]` to contants.rb.
- `src/runtime/interpreter.rb`: sibling scopes are assumed read-only; assumption was never actually double-checked.
- Add :func_signature attr to Ore::Func, and it should be an Ore::Func_Signature instance
- Structs on types. `Array<Type>`, `Dictionary<Type,Type>`
- Structured-type declarations (`String<Dictionary> {}`, `String<Number> {}`, ...)
- When declaring operators, leaving out precedence should look up the default precedence for given operator, with fallback value. `@operator == @infix 500 {l,r;}` can then become `@operator == @infix {l,r;}`
- Reference-site struct slots can now be named to disambiguate an otherwise-ambiguous type-only match, e.g. `String<other := {x=1}>()`, reusing the bare `:=` idiom.
- Added `Printable` (composable marker type with a self-documenting default `to_s`) and `Interpreter#stringify_for_display`. `@puts`, string interpolation, and `bin/ore -p` now call a declared `to_s` (own, or inherited by composing `Printable`) instead of dumping raw Ruby internals.
- Allow `@load 'file_without_extension'` by appending `.ore` if it isn't already there.
- Labeled call arguments work now: `send_greeting(to: someone)` matches a declared param label, positionally (Swift/ObjC-style, never reorders). Was dead before: parsed as a bare `:` infix, raised `Undeclared_Identifier`. Labels are opt-in at the call site; a wrong label raises `Argument_Label_Mismatch`.
- Tuple/Shape destructuring: `(a, b) := (1, 2)` / `(a, b) := <1, 2>`. Extracting more than available raises `Destructuring_Arity_Mismatch`; fewer is fine. Targets can be typed locals (checked against the value) or existing fields (`(thing.field, local) := ...`, reassigns like `thing.field = value`; must exist, non-constant, type-checked).
- Added `learn/types.ore` (declarations/construction, static `../` members, the three scope operators, the privacy convention, all four composition operators, all five type comparison operators) and `learn/shaped_types.ore` (attaching `<...>` to a Type name: variants, name-disambiguation, reference-vs-declaration timing, the spread rule; later renamed `learn/structured_types.ore`, see further down), split out of `learn/shapes.ore`, which is now scoped to Shapes (later renamed Structs) as standalone values only.
- Added 30 `learn/*.ore` files (one concept per file, on par with readme.md's coverage), surfaced two real bugs, both fixed (see bugs.md): (1) postfix `unless`/`until` put the body in the wrong branch. (2) A string literal colliding with a prefix symbol got misparsed as that operator. Also fixed: CLAUDE.md/readme.md both named a nonexistent error class; readme.md's own Operator Overloading example was internally inconsistent.
- Added `Learn_Test` (`test/learn_test.rb`): one generated test per `learn/*.ore` file, asserts none raise. Fixed `refute_raises` itself along the way (see bugs.md). Surfaced a real, pre-existing bug in `#find_operator_overload` (see bugs.md).
- `loop_control.ore`'s `.each({; return it ... })` example wasn't a bug: `return` only ever exits the function it's lexically written in, and a callback passed to another function is a different function. Rewrote that section to demonstrate and explain the function-call boundary instead of marking it broken.
- Every `@puts` across `learn/*.ore` (all 34 files) now describes its value (`@puts "label: \`expr\`"`) instead of printing a bare value with only a trailing `#` comment: easier for a beginner reading output to tell what they're looking at. Left alone where a call's own return value is already a full sentence. Corrected several stale comments found along the way. Surfaced a genuine bug, logged in bugs.md: string interpolation couldn't evaluate an expression using a custom `@operator`.
- An operator for checking AST types. For example, `func{;} ==== Func_Expr`. I'm only using four equals to illustrate. You can now use set equality `func{;} === {;}`
- `interp_bare_type_declaration`/`interp_structured_type_declaration` shared an identical ~10-line tail, extracted into `#finish_type_declaration`. Also fixed: reopening a structured variant re-ran its entire accumulated body instead of just the new expressions, like the bare path does.
- Named call arguments: `add(a := 1, b := 2)`, order-independent by declared param name; distinct from labels (`:`, positional, never reorders). Works through constructors too (`new{;}` shares `#interp_func_body`). Positional args must precede all named ones; reverting after a named one raises `Ore::Positional_Argument_After_Named`. Also: `Ore::Duplicate_Named_Argument`, `Ore::Argument_Given_By_Name_And_Position`, `Ore::Unknown_Named_Argument`.
- "Type contracts unchecked on first assignment" turned out to already work: `interp_infix_assignment` reads `expr.left.type` before falling back to the recorded `type_by_identifier`, so a non-literal RHS mismatching a `: Type` annotation raises `Type_Contract_Violation` on the very first, self-declaring assignment. No code change needed, it was just missing test coverage.
- Add `@declare` directive that accepts `Ore::Struct` and creates declarations on current scope
- Percent literals `%str(boo hoo COOL)` for ['boo', 'hoo', 'COOL'] `%sym(BOO hoo cool)` for [:BOO, :hoo, :cool], etc.
- Add `Ore::Statement`, ``` x := `1+2` ```, can be interpreted later by just calling it `x()`
- `readme.md` now documents labeled and named function arguments (previously only in `CLAUDE.md`/`learn/`).
- Four interpreter dedupes: `=<=`/`=!=` now derive from `=>=`/`===` via a shared `superset_of_types_and_structure?`; `find_in_stack` gained `excluding:` and replaced 4 inlined `stack.reverse_each.find{...}&.get(...)` copies; new `raise_missing_scope_operator_target!` replaces the `./`/`../`/else raise `case` copy-pasted 3x; new `rebind_func_to_scope` replaces 3 `dup` + `enclosing_scope=` copies in `interp_identifier`.
- Four more small interpreter redundancies: dead `next if Param_Expr` guards removed from `interp_func_body`/`interp_route_body` (params already subtracted from body first); `interp_conditional`'s `unless`/`else` copy-paste merged into one branch; `interp_circumfix`'s tuple branch collapsed empty/multi into one path using `.map` instead of `reduce([])`; `composed_types_for`'s dead intrinsics branch (identical to its `else`) removed.
- `errors.rb` no longer needs a live Interpreter reference. `Interpreter.cached_source_by_filename` is now class-level (was per-instance) so `Error_Formatter` can read a source snippet without holding a runtime; `Error#initialize` dropped the `runtime` param entirely.
- A function expression does not store arguments and body expressions separately, making you have to filter through the expressions to get the body.
- Add `@refute condition`. Both `@assert` and `@refute` are passthrough, meaning they just return the expression given.
- Every `learn/*.ore` file's `@puts` demonstrations converted to `@assert` (except `directives.ore`, which keeps a few real `@puts` calls since it's specifically demonstrating what `@puts` does). Cut console noise during `rake test` and gave each example a real, checked assertion instead of just eyeballed output. Surfaced the proxy-wrapping bug logged in bugs.md.
- `rake test`: ~50s -> ~2.3s. Cached `ore/preload.ore`'s parsed AST and type-check pass class-level instead of per-Interpreter (both were redone from scratch on every `Ore.interp` call). AoC tests (real compute, not stdlib overhead) excluded entirely by renaming `advent_of_code_test.rb` -> `advent_of_code_test_no_more.rb`, which no longer matches the Rakefile's `test/**/*_test.rb` glob.
- Bare named structs: `Named <Struct>` builds a real Struct with `.name == 'Named'`. No new grammar: `Ident<...>` already parsed as a `Type_Expr` reference, just fell through to a named struct instead of raising `Undeclared_Type_Structure` when nothing was declared under that name. `Thing := <Struct>` stays anonymous, unaffected.
- Added a forward-declaration pass (`Declarator`, wired into `Interpreter#resolve_forward_declaration`) so calling a function or referencing a type before its own definition works, including mutual recursion; plain variable assignments are deliberately excluded and still raise `Undeclared_Identifier` if read before their line runs. `@load` (bare and `Ident/IDENT := @load`) hoists too, so imports can live at the bottom of a file.
- Added `self`/`Self` keywords as sugar for `./`/`../` (bare identifier resolution, `self.x`/`Self.x` reads and writes including self-declaration during construction, `self.funk {;}`/`Self.funk {;}` bare function declarations desugared at parse time, `Self()` constructs like the type name would); now the preferred spelling over `./`/`../` in new Ore code (readme.md/CLAUDE.md/learn/ updated). Surfaced and fixed two real bugs along the way, logged in bugs.md.
- Migrated every raw `./`/`../` usage in `ore/*.ore` (stdlib) and `examples/*.ore` to `self`/`Self`, matching learn/readme/CLAUDE.md. Also fixed `self.x,`/`Self.x,` (nil-init) not parsing, sharing the same parser desugaring `self.funk {;}` already used (`parse_self_prefixed_func_name` renamed to `parse_self_prefixed_identifier`, now used by both).
- Migrated incidental `./`/`../` usage across `test/*.rb` to `self`/`Self`, kept it where tests specifically cover the operators themselves, removed the self/Self-equals-`./`/`../` equivalence test. Found a pre-existing bug logged in bugs.md.
- Split `todos.md` into `todos.md` (general/feature work), `bugs.md` (defects), and `design.md` (open language-design questions), each with their own `[DONE]` log.
- `Primary_Key {}` is now a standard marker type for `create_table` schemas, same as `String`/`Bool`. `database.rb`'s column dispatch generalized to any user enum and exhaustively lists Sequel's column types (unsupported ones commented in). `db.create_table` tags its returned `Table` with the real model type's identity when the schema matches (new `Interpreter.current`).
- Struct type annotations now work on function params, not just variables: `right: <name: String, ...>` is structural -- any argument with those members, compatibly typed, satisfies it (`Any` is a wildcard), enforced at every call.
- `Any` is a universal wildcard type: any non-nil value/type is `==`/`===` to it and back, no composition required.
- `Table`'s CRUD methods return a real, model-shaped `Struct` instead of a `Dictionary` when the model has a structured `Table` reference (`Tasks | Table<'tasks', Task> {}`); falls back to `Dictionary` otherwise. `create` also now returns the created record itself, not just the id (docs/tests updated to match).
