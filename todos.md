[LEGEND: + todo at top / - done at bottom]
+ `readme.md` doesn't document labeled *or* named function arguments at all — both exist only in CLAUDE.md/`learn/labeled_arguments.ore`/`learn/named_arguments.ore`.
+ Ability to cast and compare types at runtime. For example, a class with struct `<id: Int>` should qualify anywhere the type annotation says `: <id: Int>` (a struct with one int named `id`). Maybe even `: <Int>` should succeed whenever any Int is declared on the class. 
+ Add runtime types for all instnatiatable constructs in the language
+ Wire up `Any` type. I suppose it should == and === any other type or instance? I need to read up more on the concept of `Any`.
+ I had this idea of not crashing on division by zero, but instead returning a type that represents infinity. `Inf := Infinity | Number` and `-Inf`. I should research this.
+ Switch statement / pattern matching: required for @help / help{expr;} function.
+ Rename preload.ore to global_scope.ore. That's what the global scope will be loaded from, gives me better control. Would be cool to construct different scopes that users can pick to be their global scope, or make their own.
+ Constants can be declared with a type and without a value. The first assignment locks in the value. `VERSION: Number` and assign it once later.
+ `Int` and `Flt`/`Float`, see `ore/number.ore`
+ Rewrite the Dom_Renderer in Ore, and remove the magic rendering whenever a Dom is returned. Make the user render themselves, but implement it for them. Everyone prefers control over magic probably.
+ Table associations (`belongs_to`/`has_many`)
+ Add jsonb-like column support to databases.
+ No JSON encode/decode exposed to Ore: `require 'json'` only used internally, for example when parsing POST bodies (`interpreter.rb`).
+ No outbound HTTP client: Ore can serve requests but can't make them.
+ No `ENV`/config access: only I/O primitive is `File_System`.
+ No in-Ore testing/assert facility: Minitest only tests the interpreter itself.
+ Percent literals `%str(boo hoo COOL)` for ['boo', 'hoo', 'COOL'] `%sym(BOO hoo cool)` for [:BOO, :hoo, :cool], etc.
+ Stride overlap for `for x by n,overlap`: `getting_started.md` documents `for x reject by 2,1` / `for x each by 3,1` style overlapping chunks, but the parser doesn't support the second stride argument yet (`src/compiler/parser.rb`: "Currently `stride` doesn't support option to overlap elements"). This was meant to be implemented, not just aspirational docs: needs the parser to accept `by <stride>,<overlap>` and the interpreter's chunking (`each_slice` today) to respect the overlap instead of using non-overlapping slices.
+ `help {expr;}` or `@help` that accepts any expression and returns information about the Type, primitive, function, etc. Literally any expression should be able to be described given its AST. You even have access to the live values so the information can be dynamic to reflect what the user is actually asking about.
+ Improve error messaging. `src/runtime/error_formatter.rb` doesn't display source code properly for some expressions, I think.
+ Ensure `@cd ..` doesn't work if no scope was pushed with `@cd <scope>`. Probably need some kind of stack that remembers the pushes.
+ Rename `@cd` and `@ +/-` to something more descriptive. `@push_write <obj>`, `@pop_write <obj>`, `@push_read <obj>`, and `@pop_read <obj>`. Also rename sibling scope, and add a scope for writing.
+ Add `@declare` directive that accepts `Ore::Struct` and creates declarations on current scope
+ Direction: use Structs (Member-backed, `ore/struct.ore` + `ore/member.ore`) as the common schema mechanism across Database/Table, Server, and Dom, toward an Ore-idiomatic way of doing webdev instead of ad-hoc per-feature conventions. `structure_declaration` already lets any type declare a structure via existing `<...>` syntax (`Post<title: String, body: String> | Table {...}`), no new grammar needed to start. Concrete angles: a Table's structure doubling as both its `create_table` schema and its Record member types (replacing the current Dictionary-of-strings schema and the "query results should return Records, not raw Dictionaries" gap below); typed request/response bodies for Server routes; declared prop structs for Dom components instead of bare instance members. Blocked on: a Database-side bridge translating a Struct member's Type object to a Sequel column type string; and the `@declare <struct>` directive above, for stamping a struct's members onto `./` ergonomically.
+ Some kind of to_s{;} and inspect{;} equivalents for printing objects
+ A way to get capitalization at runtime `:Identifier, :identifier, :IDENTIFIER` from any given identifier.
+ Ability to coerce types when declaring http routes. `put://survivors/:name { name: String; }`
+ Currently `func{a;}` must be called with parens `func(a)` otherwise `func` evaluates to a pointer to this func. I want to borrow from Jai: `x := func.*` should give you the pointer, `func` and `func()` should be a calls as usual, and then somehow I need to parse arguments without parens.
+ Add another version of `@puts` that prints the expression as a string. Like `@puts! "`(1+2)`"` should print `(1+2) => 3`
+ Replace `@param` unpack syntax with `@unpack param`, or `@read param`/`@write param` matching @push/@pop, and giving more hoice
+ Add a builtin debugger, `@debug` maybe. It should stop execution and let you step through, and treat the current place as a repl that you can do whatever in. My hunch is a separate Interpreter#output, like #debug_output that is capable of interrupting the stream of expressions being interpreted. `@debug when_condition_true` would be neat too. 
+ `interp_comparison_infix`'s `=<=`/`=!=` branches duplicate `=>=`/`===`'s field-superset logic — derive them instead of keeping four blocks in sync by hand.
+ Consolidate the inline `stack.reverse_each.find { has? }&.get` copies onto `#find_in_stack`: `resolve_func_signature`, `interp_prefix`, `interp_postfix`, `#find_operator_overload`'s fallback (keeps its Type/Instance exclusion).
+ Dedupe the "scope operator with no matching scope" raise `case` (`./`/`../`/invalid), copy-pasted in `interp_identifier`, `interp_infix_assignment`, `interp_infix_declaration`.
+ Dedupe the `func.dup` + `enclosing_scope = scope` rebind, three copies inside `interp_identifier` alone.
+ Small interpreter redundancies: params are subtracted from the body *and* re-skipped with `next if Param_Expr` in both `interp_func_body`/`interp_route_body` (comments already admit it); `interp_conditional`'s `unless` branch is an admitted `@Copypaste` of the else branch (`result || nil` is a no-op); `composed_types_for`'s intrinsics branch duplicates its else, just to pre-empt the `Ore::Type` match — deserves a comment; `interp_circumfix`'s tuple branch uses `reduce` as `map` and duplicates construction across the empty/multi branches.

[BUGS]
+ String interpolation can't see custom operators declared earlier in the same program: `` `4 -> double -> add_fifteen` `` inside a string only evaluates to `4` (stops at the first unrecognized token), not `23`. `interp_string` re-parses each backtick chunk via a fresh, isolated `Ore.parse(sub)` call that has no knowledge of `@operator` declarations registered elsewhere in the actual program (that registration is a pre-scan over the *whole* top-level input, never threaded into this isolated re-parse). Workaround: assign the expression to a local first, interpolate the local instead (bare identifiers/built-in operators are unaffected). Found while making learn/operator_overloading.ore's `@puts` calls descriptive.
+ Tuple-in-tuple has infinite members: `((), true).0.1`, `.0.2`, `.0.3`... all return a Tuple instead of erroring past the actual length.
+ calling () on a dict field that shadows a builtin method but isn't callable raises a misleading error instead of a clear not-callable error. `dict := {keys: 4}, dict.keys()` raises `Cannot_Initialize_Non_Type_Identifier`
+ `<=>` works for Numbers/Strings only by accident: it falls through to `left.send('<=>', right)` in `interp_infix`, which works because number/string literals decay to plain Ruby values with a native `<=>`. Custom Instances have no `<=>` defined, so `<=>` on user-defined types raises rather than erroring cleanly or doing something sensible.
+ Cannot call any members on a bare Number: `123.numerator` (Undeclared_Identifier). This works though: `Number(123).numerator`
+ Replace `@start` with `@start_server`
+ String literals interpret to a raw Ruby `::String`, not a wrapped `Ore::String` — so per-literal metadata (e.g. `quotation_style`, added for Member/Struct display) only survives when a value is used directly at the same call site; it's gone the moment the string passes through a variable first. Want to make string literals interpret to a real `Ore::String` instance generally. Bigger than it sounds: touches every place that currently assumes a raw Ruby string (native string methods, `.is_a?(::String)` checks, Ruby proxy methods). (The raw-string-on-the-left `==` asymmetry this used to also list is fixed now — `Ore::String#to_str` closes it, see Done.)
+ `src/compiler/parser.rb`: a comma is discarded instead of implying a tuple in `#complete_expression` (possibly related to tuple unpacking).
+ I want to be able to forward declare a constant without a value, then lock its value the first time it's assigned.

[LANGUAGE DESIGN]
+ Enums are `:IDENTIFIER {}` whose members are other `Enums{}` or `CONSTANTS`
+ No try/catch, or any error handling: unhandled runtime errors crash the program outright. Ore errors are Ruby exceptions under the hood already, so the interpreter just needs to catch and hand off to a user-defined handler. Matters most for web routes, file I/O, DB calls.
+ `src/runtime/interpreter.rb`: builtins aren't user-extensible yet; a rough shape of the requirements exists in some comment but nothing's implemented. Requirements for this to work might include: 1) creating a new Type in Ore. 2) creating proxy class in scopes.rb or preferrably a custom directory. 3) Functions which use the @ruby expression in its body should be named something like Ore::Type "proxy_#{func_name}"
+ What should the default value for uninitilized declarations be? `x: Number` should probably start as 0 instead of nil. The value should depend on the type as well, like a String should be "" by default. Etc.
+ `@valid` directive: validate whether a call would type-check without actually invoking it, e.g. `@valid identity(123)` #=> true, `@valid identity("1")` #=> false. Return-type syntax now exists, so this is unblocked: just needs the directive itself: check arg count/types against a real function's `param_types`/`return_type` (both now on `Ore::Func`) without invoking it.
+ Related idea, tabled for now: an operator to structurally match a function's param signature against a shape, e.g. `add ==== {Number, Number;}`.
+ Type contracts (`: Type` annotations, both nominal and signature) are only ever checked on *reassignment* (`interp_infix_assignment`): never on the first, self-declaring assignment. `to_string: Num_to_str = { totally wrong shape }` isn't caught. Pre-existing architectural gap, not new: applies equally to plain nominal types.
+ Structural typing via `<...>` (Struct) used as a type annotation instead of attached to a type name: `funk { a, b -> <id: Number>; ... }` would mean "returns anything with at least an `id: Number` declaration" (checked against the value's own declarations, structurally — not against a registered Type's name). Distinct from the *existing* meaning of `<...>` right after a type name (`Abc<Number>`), which stays "structure of `.structure`" — same syntax, meaning determined by position. Decisions so far: 1) duck-typing against actual declarations is intentional — a plain `{id: 5}` Dictionary should satisfy `<id: Number>` too, not just instances of a matching Type. 2) A Func-shaped member meaning "must respond to X" (interface-style) is a good later idea, not now — data-only for now. 3) Runtime-only enforcement for now (reuse the existing return-type `Type_Contract_Violation` check path), not the static `Type_Checker`. Grammar sites in `src/compiler/parser.rb` that only accept a bare `:Identifier` today and would need to accept a `<...>` literal too: `parse_func`'s inline `-> Type` return type; `parse_func`'s own param `: Type` annotations (two spots — named params, and bare-signature-style params); `parse_signature_literal`'s param types (two spots, same bare-signature-style shape); `parse_identifier_expr`'s general `: Type` annotation (variable declarations, e.g. `x: Number = ...`) — this last site *already* has a `: <...>` grammar path today (`expr.type_struct`, "bare struct annotation," currently just stored, never enforced — see the `# 7/20/25, I'm storing the type as well, even though I haven't written any code to support types yet.` comment) that this would need to reconcile with rather than duplicate.
+ `src/compiler/type_checker.rb`: static checking only covers bare `identifier(args)` calls, not method/dotted calls (`app.servers.push(123)` isn't checked at all) — `check_call` requires `expr.receiver.is_a? Ore::Identifier_Expr`, `check_infix` doesn't even recurse into `.`-chains, and `register_func`'s signature registry is a flat hash keyed by bare name anyway (would collide across e.g. `Array<Web_Server>`/`Array<Database>` variants of `push`). Found via `temp/web_application.ore`.
+ `test/regression_test.rb`: plan to make bare `{x}` implicitly set `x` if it's declared in the scope, so `x=123, {x}` should set x to 123.
+ `src/shared/constants.rb`: `UNPACK_OPERATOR = '@'` collides conceptually with `Ore::BUILTIN_OPERATOR` (also `@`); pick a distinct symbol for one of them.
+ `ore/preload.ore`: `Iterable` composition exists but is unused; consider making it behave like a Swift-style protocol.
+ Consider disallowing ! and ? to be part of identifiers. Currently all identifiers may end with ? or ! like Ruby.
+ Allow control of initializer order for compsitions. Composition types are already known by identifier so I could call Comp.new() inside of new{;}. The interpreter would need to be aware that the top scope is new{;} and not to just instantiate a new Instance into new{;}'s scope.
+ Design question: a composed-in member default (`App | Server { port := 80 }`) is silently overwritten if the composed type's inherited `new` runs and reassigns that member to its own default (`Server`'s `new { port := 8080; ./port = port }`), since `App` never defines its own `new`. Found via `ore/examples/todo_app.ore`. Is inheriting the constructor as-is correct (own body must define `new` to protect a member), or should a member default declared directly in the composing type's body take precedence over an inherited constructor's default?
+ Is there a way to namespace without strings? Like `Island/Hatch/Computer` instead of `Island.Hatch.Computer`. I'm not sure if this is even necessary, but I'm curious if this can be implemented in a way that it's able to coexist with the arithmetic division operator `/`.
+ Rename language to `Lost`? Rename extension to `preload.lost`
+ `src/external/ruby/table.rb`: query results should convert to a `Record` instance instead of raw data.
+ `src/external/ruby/table.rb`: `#insert` should maybe return `self` or a hash of the inserted row instead of just the id.
+ `ore/database.ore`: `connection` member is typed as raw `Sequel::SQLite::Database`, needs a real Ore type.
+ `ore/date_time.ore`: no `Date_Time` class in `scopes.rb` yet.
+ `test/type_checker_test.rb`: no error raised for an identifier with an unknown type on the RHS; maybe should warn.
+ `test/parser_test.rb`: design question of whether `return` should be a prefix keyword instead of producing a `when_true`, and whether `when_true`/`when_false` naming should better convey that they return arrays.

[DONE]
- `src/compiler/type_checker.rb`: `type_by_identifier`/`func_signatures_by_identifier` were flat hashes keyed by bare name, scope-blind — an unrelated same-named local in one function poisoned inference in another (`f1 { x: Number = 5 }` falsely broke an unrelated `add(x)` call in `f2 { x := "hello" }`). Fixed with a real scope stack, searched innermost-first like the interpreter's own identifier resolution.
- A trailing `#` comment on the last line of a function or program body silently becomes the return value instead of being discarded. Repro: `add { a, b; a + b  # sum them }` then `add(4, 8)` returns the comment `"sum them"` instead of `12`
- Add extra period in the middle of range operators .. -> ...
- `Array#map` crashes if the anonymous function doesn't explicitly declare `it`/`at` as params (`arr.map({; it * 2 })` fails; `arr.map({it, at; it * 2})` works): documented inline in `ore/array.ore`.
- Nil-safe access: `x.?method` should return nil instead of raising when `x` is nil.
- Validate whether the precedence is even used when declaring an operator. Yes, it is.
- `src/runtime/interpreter.rb`: array `<<` is special-cased in the interpreter instead of being a real operator declaration on `Array`; revisit once operator declarations exist.
- Add Type set comparison operators
- Function signature literals (`Type{Param;}` as a value/alias, `name: Type{Param;}` self-declaring): `Ore::Func_Signature`, with `Invalid_Func_Signature` raised for malformed ones.
- Return-type annotations on real functions: `name: Type {}` prefix and `{params -> Type; ...}` inline (works for anonymous functions too): plus runtime enforcement (`Type_Contract_Violation` if the actual return value doesn't match).
- Structural signature matching: reassigning a signature-aliased identifier now checks a real function's actual param/return types against the alias, not just nominal string equality.
- Rename @use to @load? Or something else because "use" doesn't clearly explain the behavior.
- Replace all `# todo: Proper error` placeholders scattered through `src/runtime/interpreter.rb`: real error types needed instead of generic ones. And add tests. A few of the errors are unreachable because of other guards, but the ones that are have a test now.
- Change set comparison operators to `=== =!= =/= =>= =<=` from `=== !== =/= >== ==<` to be more consistent
- `src/compiler/lexer.rb`: Add operator symbol exclusion list `' " { } ( ) [ ]` to contants.rb.
- `=~`/`!~` (regex match) are listed in `COMPARISON_OPERATORS` (`src/shared/constants.rb`) but missing from `INFIX`, so the parser never builds an `Infix_Expr` for them: same bug class the `!==` fix addressed. `'abc' =~ 'xyz'` silently parses as disconnected expressions instead of erroring. Needs `=~`/`!~` added to `INFIX`, and an actual regex-match implementation (currently nothing in `interp_infix`'s `COMPARISON_OPERATORS` branch handles them beyond the `left.send` fallback).
- Function param declarations should use `:=` to be consistent with the remainder of the declaration syntax
- `src/runtime/interpreter.rb`: sibling scopes are assumed read-only; assumption was never actually double-checked.
- Add :func_signature attr to Ore::Func, and it should be an Ore::Func_Signature instance 
- Structs on types. `Array<Type>`, `Dictionary<Type,Type>`
- Structured-type declarations (`String<Dictionary> {}`, `String<Number> {}`, ...)
- Route registration bug found while investigating the above: a named-handler route (`get://path some_handler`) was keyed in the route table by the handler function's *name* instead of `method:path`, so two different routes sharing one handler silently collided and the earlier one was dropped. Always keyed by `method:path` now.
- Bare annotated properties (`x: Number`) only self-declared on the Type at declaration time, not per-instance, so assigning to one inside `new` before ever reading it first raised `Cannot_Assign_Undeclared_Identifier`. Now self-declares eagerly per-instance, matching how the comma idiom (`x,`) already worked.
- `Array#include?` was a raw `@ruby` proxy, so it compared elements with Ruby's native `==` and never saw an Ore-level `@operator ==` overload on a custom Instance (e.g. two structurally-equal `Member`s compared unequal). Converted to an Ore implementation (same pattern as `find`/`any?`/`all?`), so it goes through the interpreter's own `==` dispatch. Same class of bug as the `<=>` entry above -- worth checking other `@ruby`-proxied `Array`/`Dictionary` methods that rely on Ruby's native comparison for correctness against custom Instances, `uniq` (relies on Ruby's `==`/`hash`) being the most likely next offender.
- When declaring operators, leaving out precedence should look up the default precedence for given operator, with fallback value. `@operator == @infix 500 {l,r;}` can then become `@operator == @infix {l,r;}`
- Bare `return` (no value) raised `Interpret_Expr_Not_Implemented` instead of yielding `nil`.
- `.?` only swallowed a missing member on plain Scope receivers — `[].?x`/`{}.?x`/`(1..5).?x` still raised, and `Array.?uniq` (instance method looked up on the bare Type) raised `Cannot_Call_Instance_Member_On_Type` unswallowed. Now covers all receiver kinds and both error types.
- `interp_dot_range` silently returned the range itself for any `.member` besides `.each` instead of raising. Now routes through the same `interp_dot_scope` path Array/Tuple use; `Ore::Range` gained a `has?` stub so that path doesn't crash with `NoMethodError`.
- Func signature literal syntax changed to match real functions: `Return_Type{Params;}` → `{Params -> Return_Type;}`. The old return-type-before-brace prefix (`name: Type { }`) is retired entirely, including for self-declaring signatures — `double: Number{Number;}` now raises `Invalid_Func_Signature`; use `double: {Number -> Number;}` instead.
- Structured-type-variant storage redesigned: was a single mangled string key (`"String<Dictionary>"`, types only, never names) — two declarations differing only by slot name silently collided/merged into one variant. Now a per-scope list (`Scope#structured_type_variants`), matched by real structure equality (`Ore::Struct#structure_declaration_equal?`/`#satisfied_by_candidates?`) instead of a string.
- Reference-site struct slots can now be named to disambiguate an otherwise-ambiguous type-only match, e.g. `String<other := {x=1}>()`, reusing the bare `:=` idiom.
- Fixed the "lone unnamed Struct value spreads" rule firing at reference sites too, not just declarations — `Table_With_Opts<Options>` (bare reference) used to explode `Options` into its own fields instead of treating it as one opaque value, so it could never match a named-slot declaration. References now never spread; only declarations do.
- Be able to instantiate Structs: `<name: String, age: Number>('Alice', 30)` now works. `<...>()` (calling a bare struct literal) was completely broken before (`interp_struct_call` referenced undefined locals, always raised `NameError`) — now builds a real instance with the call's arguments as per-slot values.
- Renamed `Member` → `Field` throughout (Ore: `ore/field.ore`, Ruby: `field.rb`/`Ore::Field`); `Shape` (later renamed to `Struct`, see further down) kept its own name at the time. Renamed all "slot" terminology to "field" across the codebase (interpreter.rb, shape.rb, parser.rb, expressions.rb, scopes.rb, type_checker.rb, tests, docs), except the two unrelated pre-existing uses (function-signature params, and an `errors.rb` comment about an exception's own `expression` attribute).
- Added `Printable` (composable marker type with a self-documenting default `to_s`) and `Interpreter#stringify_for_display`. `@puts`, string interpolation, and `bin/ore -p` now call a declared `to_s` (own, or inherited by composing `Printable`) instead of dumping raw Ruby internals.
- `Array#==` was Ruby-native (`values == other&.values`), so it never saw an Ore-level `@operator ==` on element Instances — same bug class as the `include?` fix above. Added a real `@operator ==` to `ore/array.ore`. Separately: `!=` never derived from a declared `==` anywhere in the language — any type declaring `==` but not `!=` silently fell back to Ruby identity for `!=`. Fixed generally: `!=` now derives from a declared `==` (negated) when it has no overload of its own.
- Added `to_s{;}` to `Array`, `String`, `Dictionary`, `Bool`, `Nil`, `Tuple`. Surfaced a chain of real, previously-hidden bugs along the way: Dictionary/Tuple literals never linked `enclosing_scope` at all (so no real Ore-declared method — not just `to_s` — could ever be found on one, only `@ruby` proxies could via their separate by-class-name fallback); Tuple's own construction (untouched since first written) passed the type-name string `'Tuple'` as its `values` constructor arg by mistake, and the later `tuple.values = values` only reassigned the Ruby attr, never `@declarations['values']` (what Ore-level identifier lookup actually reads) — so `t.values` on a real Tuple returned the stale string `"Tuple"` forever; `nil`/`true`/`false` were never linked to their declared types either; `Ore::Nil` was `< Scope` instead of `< Instance`, breaking the enclosing_scope method-lookup fallback structurally (that fallback is gated on `is_a?(Ore::Instance)`); and `Ore::Bool::TRUE`/`FALSE` were defined *above* `def initialize` in the same class body, so Ruby silently used the wrong inherited initializer (`Type#initialize`) instead, and `truthiness`/`@declarations['truthiness']` were never actually set for either singleton — both compared falsy.
- `parse_type_decl`'s composition-chain loop (`until curr? '{'`) had no exit besides finding one — any composition typed with no `{ }` body (`A & B`, any operator) hung the parser completely, forever. Turns out that's a legitimate pattern, not an error: a body-less composition chain is now a value, building an anonymous, unnamed `Type` by applying the chain (`(Castaway & Other)()`, exactly as if `X | Castaway | Other { }` had been written for some unnamed `X`) — instantiable, comparable via `===`/`=>=`/etc, assignable, same as any named composed type.
- Added `learn/types.ore` (declarations/construction, static `../` members, the three scope operators, the privacy convention, all four composition operators, all five type comparison operators) and `learn/shaped_types.ore` (attaching `<...>` to a Type name: variants, name-disambiguation, reference-vs-declaration timing, the spread rule; later renamed `learn/structured_types.ore`, see further down) — split out of `learn/shapes.ore`, which is now scoped to Shapes (later renamed Structs) as standalone values only.
- Removed `Ore::Shape`'s `:defaults` hash — `names`/`type_names`/`types`/`values` mirrored exactly between `shape.rb` and `ore/shape.ore` now, `values` holding each field's real value (`nil` if none). Fixed a clobbering bug this surfaced: `build_shape` only re-asserted `.fields` after the declared `Shape` type's own body ran (clobbering the rest back to empty) — now re-asserts all four.
- Renamed Ruby-only `tag_types` reader to `type_objects` (exists to dodge shadowing inherited `Type#types`; name now says what it returns).
- `.shape` on a tagged type reference (`Abc<value>`) bypassed `#build_shape`, so `.shape.fields` came back empty. Now goes through `#build_shape` like every other shape construction.
- `#build_shape`'s Field loop skipped unnamed fields, dropping them from `.fields` and desyncing later indices from `names`/`types`/`values`. Every field slot gets a `Field` now (unnamed = nil name).
- Parser: standalone `<...>` shape literal failed to parse unless its first field looked like a type name (`<1+2+3/123>` broke, `Abc<1+2+3/123>` didn't). Fixed by treating any leading `<` in expression-start position as a shape literal unless a custom prefix operator claims it. Noted, not fixed: custom prefix operators (`<`, `~neg`) were already broken pre-existing on `main`.
- Allow `@load 'file_without_extension'` by appending `.ore` if it isn't already there.
- Labeled call arguments work now: `send_greeting(to: someone)` matches a declared param label, positionally (Swift/ObjC-style, never reorders). Was dead before — parsed as a bare `:` infix, raised `Undeclared_Identifier`. Labels are opt-in at the call site; a wrong label raises `Argument_Label_Mismatch`.
- Retired `identifier: Type { ... }` syntax is now just unparseable as a function, not specially rejected. Fixed two regressions this surfaced along the way: self-declaring signatures (`double: {Number -> Number;}`) lost their dispatch clause; `Func_Signature#matches?` wrongly accepted another bare signature as satisfying a signature-typed identifier — now only a real matching `Func` can.
- Circumfix groupings (`()`, `[]`, call args) had the same bare-identifier-before-`,` ambiguity `<...>` was already fixed for: `foo(missing, 5)` silently turned `missing` into `nil` instead of raising `Undeclared_Identifier`. Fixed the same way.
- Tuple/Shape destructuring: `(a, b) := (1, 2)` / `(a, b) := <1, 2>`. Extracting more than available raises `Destructuring_Arity_Mismatch`; fewer is fine. Targets can be typed locals (checked against the value) or existing fields (`(thing.field, local) := ...`, reassigns like `thing.field = value` — must exist, non-constant, type-checked).
- Strict field creation: `.` can no longer fabricate an undeclared field (`Number.yolo = 123` used to "succeed" then be unreadable; `Number.yolo := 123` silently hit the wrong scope). `./`/`../` self-declaration from inside a type's own method is still legit, but only while under construction — a later method self-declaring a new field now raises too. One shared impl (`#assign_dot_field`) for all `.`-writes.
- Fixed Field/Shape display: an unnamed field's `type` slot held its own raw value (Field showed `<1>` as `"1"` instead of `"Number"`), and a nil field name printed a stray `"nil: "` prefix. A String value now displays quoted, matching whichever quote char its literal was written with (`String_Expr#quotation_style`, captured at lex time by the lexer/`Lexeme`) — narrow fix for now: only survives when the string is used directly at its own call site (wrapped into a real `Ore::String` right there, see `#wrap_shape_string_value`), not through a variable. Surfaced a pre-existing, unrelated bug along the way: `Ore::String` never overrode `==`, so two same-content instances (or a wrapped value vs. a raw literal) were never equal — fixed with real content comparison on `Ore::String` itself.
- Added 30 `learn/*.ore` files (one concept per file, on par with readme.md's coverage) — surfaced two real bugs, both fixed: (1) postfix `unless`/`until` (`expr unless cond`, `expr until cond`) put the body in `when_false` instead of `when_true` like the block form does — ran `unless` backwards and made `until` hang forever (empty loop body). (2) The parser's prefix-operator check matched by value only (needed for `return`/`not`, lexed as plain identifiers) with no type guard, so a string literal whose content collided with a prefix symbol (`'!'`, `'-'`, `'return'`) got misparsed as that operator instead of the string value — `"hi!".end_with?('!')` raised `Interpret_Expr_Not_Implemented`. Also fixed: CLAUDE.md/readme.md both named a nonexistent error class (`Cannot_Reassign_Undeclared_Identifier` — real name is `Cannot_Assign_Undeclared_Identifier`); readme.md's own Operator Overloading example was internally inconsistent (reused `->` for two different global overloads in the same flow, so the second silently overwrote the first — claimed `5 -> double` returns `10`, actually returns `999`).
- Closed the remaining `Ore::String#==` asymmetry: a raw Ruby string literal on the *left* (`message == File_System.read(...)`, where the read comes back wrapped) still hit Ruby's own native `String#==` instead of `Ore::String#==`. Added `#to_str` — Ruby's own `String#==` already delegates to `object == self` for a right-hand object that responds to `to_str`, landing back on `Ore::String#==` correctly either direction. Found via `learn/file_io.ore`'s own `@assert message == content` failing unexpectedly.
- Added `Learn_Test` (`test/learn_test.rb`) — one generated test per `learn/*.ore` file, asserts none raise. Fixed `refute_raises` itself along the way: called with no explicit exception class, `rescue *[] => e` catches nothing at all (not the same as a bare `rescue`) — silently a no-op everywhere it was already used that way. Surfaced a real, pre-existing bug: `#find_operator_overload` treated a bare Type object the same as an instance (`operand.is_a?(Ore::Scope)`, not `Ore::Instance`), so comparing two Type objects directly (`left.type == right.type`, e.g. Field's own `==` comparing two named fields' declared types) wrongly dispatched into the type's *instance*-level `==` overload with the Type objects as `left`/`right` — `left.value` on a bare Type raised `Cannot_Call_Instance_Member_On_Type`. Found via `learn/shapes.ore` comparing two same-shaped Shapes with `==`.
- `loop_control.ore`'s `.each({; return it ... })` example wasn't a bug — `return` only ever exits the function it's lexically written in, and a callback passed to another function is a different function. Rewrote that section to demonstrate and explain the function-call boundary instead of marking it broken.
- Every `@puts` across `learn/*.ore` (all 34 files) now describes its value (`@puts "label: \`expr\`"`) instead of printing a bare value with only a trailing `#` comment — easier for a beginner reading output to tell what they're looking at. Left alone where a call's own return value is already a full sentence (e.g. `gather_water()` returning `"Gathered 1 liter(s) of water"`). Corrected several stale comments found along the way (didn't match actual output). Surfaced a genuine bug, logged separately above: string interpolation can't evaluate an expression using a custom `@operator` (`learn/operator_overloading.ore`) — worked around there by assigning to a local first and interpolating that instead.
- An operator for checking AST types. For example, `func{;} ==== Func_Expr`. I'm only using four equals to illustrate. You can now use set equality `func{;} === {;}`
- Dot-infix double-interpretation: `interp_dot_infix` interpreted `expr.left`, then the Array/Range/Dictionary/scope sub-handlers each re-interpreted it from scratch (up to 3×), so a receiver with side effects ran multiple times. Sub-handlers now take the already-interpreted receiver; the `@copypaste` `else` block in `interp_dot_infix` collapsed back into `interp_dot_scope`.
- `interp_bare_type_declaration`/`interp_structured_type_declaration` shared an identical ~10-line tail — extracted into `#finish_type_declaration`. Also fixed: reopening a structured variant re-ran its entire accumulated body instead of just the new expressions, like the bare path does.
- Operator-overload dispatch double-evaluated operands: `interp_infix` interpreted them to find the overload, then handed the raw expressions to `interp_func_body`, which interpreted them again. Fixed at all three sites (arithmetic, comparison fallback, custom-operator) via `#interp_func_body`'s new `arg_values:` param. Same bug class as the dot-infix fix above.
- Bare `X.new` was a drifted partial copy of `X()`: `interp_dot_new` built the instance by hand (no Ruby-class backing, no `enclosing_scope` link, no `.structure` binding) and skipped `new{;}` entirely. Now delegates to `interp_type_call` (shared construction in `#build_instance_of_type`), so it's fully equivalent to `X()` — a required-param constructor now raises `Missing_Argument` instead of returning a half-built instance.
- `interp_for_loop`'s `Ore::Struct` branch called `types_by_field_name`, a method that never existed — `for` over a Struct always raised `NoMethodError`. Now iterates `.members` (`Array<Member>`, via `ore/struct.ore`, loaded by default); only `load_standard_library: false` still hits the empty fallback.
- Renamed `Shape` → `Struct` throughout (`shape.rb`/`Ore::Shape` → `struct.rb`/`Ore::Struct`; `ore/shape.ore` → `ore/struct.ore`), and "shaped type"/"tagged type" → "structured type" everywhere, including the individual-field vocabulary (`tag_default`, `single_tag`, etc. — later renamed again to `member_*`, see below). The Ore-level accessor moved from `.shape` to `.structure`. Files renamed to match: `shapes_test.rb`→`structs_test.rb`, `learn/shapes.ore`→`structs.ore`, `learn/shaped_types.ore`→`structured_types.ore`, `learn/shapes_and_tuples.ore`→`structs_and_tuples.ore`, `ore/examples/tagged_types.ore`→`structured_types.ore`. Surfaced a landmine: `lexeme.rb` built `Lexeme` via bare `Struct.new(...)` — inside `module Ore`, once `Ore::Struct` exists that resolves to the wrong class (same gotcha as `Array` in `Type_Checker`) — now `::Struct.new(...)`.
- Renamed `Field` → `Member` throughout (`field.rb`/`Ore::Field` → `member.rb`/`Ore::Member`; `ore/field.ore` → `ore/member.ore`) — "struct member" reads better than "struct field". `.fields` → `.members`; the Struct-literal vocabulary (`field_default`, `single_field`, etc.) followed. CLAUDE.md's "Field Creation Is Strict" section and `learn/field_creation_is_strict.ore` renamed too, since that rule predates Struct entirely and was already describing what CLAUDE.md's "Static Declarations" section calls "members". Unrelated "field" uses (Dictionary keys, HTML form fields, `Input_Field`) left alone.
- A struct reference member named via `:=` (`Table<columns := cols, database := db>`) bound the member's own *resolved type* onto `.structure`, not the real value — `interp_type` read `supplied.type_objects` where it should've read `supplied.values`. Also strengthened `test_reference_to_struct_valued_identifier_does_not_spread_regression`, which was passing by riding this same bug.
- Named call arguments: `add(a := 1, b := 2)`, order-independent by declared param name — distinct from labels (`:`, positional, never reorders). Works through constructors too (`new{;}` shares `#interp_func_body`). Positional args must precede all named ones; reverting after a named one raises `Ore::Positional_Argument_After_Named`. Also: `Ore::Duplicate_Named_Argument`, `Ore::Argument_Given_By_Name_And_Position`, `Ore::Unknown_Named_Argument` (checked before binding, so a typo isn't masked by an unrelated `Missing_Argument`). Fixes a real footgun along the way: `add(a := 1, b := 2)` used to silently declare `a`/`b` as caller-scope locals and pass them positionally, only "working" by coincidence of matching param names/order.
- A lowercase value right after `:` in a struct member (`<columns: cols>`) used to parse silently: struct members only have `name: Type`/`name := value`, so `#parse_identifier_expr`'s `: Type` lookahead declined the `:` (never a valid type) without consuming it, and the next loop iteration reparsed the leftover `: cols` as an unrelated `:symbol` prefix literal — `columns, :cols`, two elements, no error. `#parse_struct` now raises `Ore::Invalid_Struct_Member_Annotation` when this happens. Also fixed, unrelated: `it.names` was pushing the whole element object instead of `element.value` (the name string), breaking most struct-name assertions — found while chasing down what looked like fallout from the new check but wasn't.
