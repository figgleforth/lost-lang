+ Rethink `./`/`../` as positional stack climbers (`./` = current frame, `../` = one up, chainable) instead of role-based; pure lookup, no auto push/pop, hands back a Scope you can `@push_scope`/`@pop_scope` yourself. Phase 1: only real pushed frames climb (func/type/instance/for), `if`/`while`/`until` stay flat like today. Phase 2 (later): give `if`/`while`/`until` their own frame too, reconciled with Declarator's conditional-flattening. Also decide: climbing past the bottom of the stack should error, not return nil.
+ Turn `Number{}` from joke into real number system. It would also be cool to support any system that's incrementable in any way. Imagine a 52 card deck class. `deck := Standard_Deck(), card: Standard_Card = deck.random()`, say it's 2 of hearts. `card += 1` card is now 3 of hearts.
+ How can types represent optional or required? `Type?` could be it or nil. `Type!` must be Type
+ Get rid of comma-based nil-init. Make commas delimiters again to simplify circumfixes. Make class the only place you can declare with just the identifier `Type { ident }` where it becomes `ident: Any`
+ Annotating an expression as `Expression` type, like `x: Expression = run.some.piece_of_code()` which gives you back the AST of that expression as instances of its equivalent nodes written in Ore. Like `Infix | Expression {}`, etc.
+ Type semantics that need deciding before the related todo can actually be implemented correctly (grouped together, was scattered):
  + Wire up `Any` type. I suppose it should == and === any other type or instance? I need to read up more on the concept of `Any`.
  + Formalize what it means for any type to be zeroed/nil'd. For example, `x: Int` equals `nil` after this declaration but should equal 0. Maybe some kind of `default_value{;}` that any class/type can declare. (Asked twice, same question: "What should the default value for uninitilized declarations be? `x: Number` should probably start as 0 instead of nil. The value should depend on the type as well, like a String should be "" by default.")
  + You should be able to prevent a function from returning anything. `f {->Nil;}` or `Void` or `None` or some other name. Needs deciding whether that's a real enforced type or just documentation before `-> Nil` can mean anything precise.
  + Enums are `:IDENTIFIER :: :Type {}` whose members are other `:ENUMS :: {}` or `CONSTANTS`. You should be able to access enum values by dot access as long as the left side knows the enum so it can iterate it. `x: ENUM = ENUM.ABC` becomes `x: ENUM = .ABC`. Dot-access resolution ("as long as the left side knows the enum") is still hand-wavy.
  + Design question: a composed-in member default (`App | Server { port := 80 }`) is silently overwritten if the composed type's inherited `new` runs and reassigns that member to its own default (`Server`'s `new { port := 8080; self.port = port }`), since `App` never defines its own `new`. Found via `examples/todo_app.ore`. Is inheriting the constructor as-is correct (own body must define `new` to protect a member), or should a member default declared directly in the composing type's body take precedence over an inherited constructor's default?
  + `Int` and `Flt`/`Float`, see `ore/number.ore`
  + I had this idea of not crashing on division by zero, but instead returning a type that represents infinity. `Inf := Infinity | Number` and `-Inf`. Can't implement sensibly without deciding how it participates in arithmetic/comparison first (does `Inf <=> 5` work? does `Inf == Inf`?). I should research this.
  + `ore/number.ore`: `denominator` (and `type`, e.g. `:integer`/`:float`, set at construction but never read anywhere) are vestigial across all of `Number`. Every arithmetic operator (`+`/`-`/`*`/`/`/etc., `src/external/ruby/number.rb`) and the comparison operators (`<=>`/`</>/<=/>=`) only ever look at `numerator`. A real fix needs every operator to agree on what a `Number` actually means (true rational via cross-multiplication for comparison, fraction-preserving arithmetic, or auto-reduce), not a one-operator patch. Left as-is on purpose: `<=>`/`<`/`>`/`<=`/`>=` are the canonical example of `@operator` usage, kept simple.
  + Research how other languages actually treat their Number type (int/float/rational split? one unified numeric tower? how does denominator/rational representation work, if at all?) before deciding what `Number`/`numerator`/`denominator`/`type` should really mean in Ore.
+ Think about what kind of Error system do I want. Ruby's is an obvious choice to copy, since I'm familiar with it. But I also like how Tsoding tends to use API that give you an error message or bool. `result, ok := some_thing()`
+ Ability to extract a member based on its type when name collisions exist. `dict := {keys: 4}, dict.keys()` fails because you cannot call the value 4 that you'd get from `dict.keys` in this case, as it shadows the function `keys` due to how keys in a dict work at the moment. It would be neat to do `keys: Number = dict.keys` and `keys: Func = dict.keys`
+ Jai uses `=` when passing named arguments, I use `:=` which does not make sense. But it's okay for now
+ Since private members require leading underscores, you cannot mark an entire expression as private.
+ `Statement`s are the push toward making the language buildable in itself. Eventually I wanna port all classes and types to Ore. You should be able to build any expression you want with Ore, then evaluate that expression in Ore
+ Memoized Statement syntax so you don't have to manually use the Memoizer class?
+ Design idea, still thinking it over: retire `new{;}` and give Type its own func-like body `{params; body}` so the body becomes its constructor directly, same shape as `Func`. Sketched in `temp/sandbox.ore`/`temp/new_string.ore`.
+ Direction: use Structs (Member-backed, `ore/struct.ore` + `ore/member.ore`) as the common schema mechanism across Database/Table, Server, and Dom, toward an Ore-idiomatic way of doing webdev instead of ad-hoc per-feature conventions. `structure_declaration` already lets any type declare a structure via existing `<...>` syntax (`Post<title: String, body: String> | Table {...}`), no new grammar needed to start. Concrete angles: a Table's structure doubling as both its `create_table` schema and its Record member types (replacing the current Dictionary-of-strings schema and the "query results should return Records, not raw Dictionaries" gap in bugs.md); typed request/response bodies for Server routes; declared prop structs for Dom components instead of bare instance members. Blocked on: a Database-side bridge translating a Struct member's Type object to a Sequel column type string; and the `@declare <struct>` directive, for stamping a struct's members onto `self` ergonomically.
+ Structured-type member typed `Statement` (see `learn/statement_expressions.ore`) fits a Table column default computed at insert-time, e.g. `created_at: Statement` (blocked on Date_Time existing, see below). Already works structurally today. Caveat: not idempotent, rereading the same instance re-runs it.
+ No try/catch, or any error handling: unhandled runtime errors crash the program outright. Ore errors are Ruby exceptions under the hood already, so the interpreter just needs to catch and hand off to a user-defined handler. Matters most for web routes, file I/O, DB calls.
+ `src/runtime/interpreter.rb`: builtins aren't user-extensible yet; a rough shape of the requirements exists in some comment but nothing's implemented. Requirements for this to work might include: 1) creating a new Type in Ore. 2) creating proxy class in scopes.rb or preferrably a custom directory. 3) Functions which use the @ruby expression in its body should be named something like Ore::Type "proxy_#{func_name}"
+ `@typeof` or `instance.some_type_of_membber` that tells you the type of something. Or maybe `@types` that gives the types of the current scope, and you call it like `instance.@types`? That actually seems fair.
+ `@valid_call?` directive: validate whether a call would type-check without actually invoking it, e.g. `@valid_call? identity(123)` #=> true, `@valid_call? identity("1")` #=> false. Return-type syntax now exists, so this is unblocked: just needs the directive itself: check arg count/types against a real function's `param_types`/`return_type` (both now on `Ore::Func`) without invoking it.
+ Related idea, tabled for now: an operator to structurally match a function's param signature against a shape, e.g. `add ==== {Number, Number;}`.
+ Structural typing via `<...>` (Struct) used as a type annotation instead of attached to a type name: `funk { a, b -> <id: Number>; ... }` would mean "returns anything with at least an `id: Number` declaration" (checked against the value's own declarations, structurally, not against a registered Type's name). Distinct from the *existing* meaning of `<...>` right after a type name (`Abc<Number>`), which stays "structure of `.structure`", same syntax, meaning determined by position. Decisions so far: 1) duck-typing against actual declarations is intentional: a plain `{id: 5}` Dictionary should satisfy `<id: Number>` too, not just instances of a matching Type. 2) A Func-shaped member meaning "must respond to X" (interface-style) is a good later idea, not now: data-only for now. 3) Runtime-only enforcement for now (reuse the existing return-type `Type_Contract_Violation` check path), not the static `Type_Checker`. Grammar sites in `src/compiler/parser.rb` that only accept a bare `:Identifier` today and would need to accept a `<...>` literal too: `parse_func`'s inline `-> Type` return type; `parse_func`'s own param `: Type` annotations (two spots: named params, and bare-signature-style params); `parse_signature_literal`'s param types (two spots, same bare-signature-style shape); `parse_identifier_expr`'s general `: Type` annotation (variable declarations, e.g. `x: Number = ...`), this last site *already* has a `: <...>` grammar path today (`expr.type_struct`, "bare struct annotation," currently just stored, never enforced) that this would need to reconcile with rather than duplicate.
+ `src/compiler/type_checker.rb`: static checking only covers bare `identifier(args)` calls, not method/dotted calls (`app.servers.push(123)` isn't checked at all): `check_call` requires `expr.receiver.is_a? Ore::Identifier_Expr`, `check_infix` doesn't even recurse into `.`-chains, and `register_func`'s signature registry is a flat hash keyed by bare name anyway (would collide across e.g. `Array<Web_Server>`/`Array<Database>` variants of `push`). Found via `temp/web_application.ore`.
+ `test/regression_test.rb`: plan to make bare `{x}` implicitly set `x` if it's declared in the scope, so `x=123, {x}` should set x to 123.
+ `ore/preload.ore`: `Iterable` composition exists but is unused; consider making it behave like a Swift-style protocol.
+ Consider disallowing ! and ? to be part of identifiers. Currently all identifiers may end with ? or ! like Ruby.
+ Allow control of initializer order for compsitions. Composition types are already known by identifier so I could call Comp.new() inside of new{;}. The interpreter would need to be aware that the top scope is new{;} and not to just instantiate a new Instance into new{;}'s scope.
+ Is there a way to namespace without strings? Like `Island/Hatch/Computer` instead of `Island.Hatch.Computer`. I'm not sure if this is even necessary, but I'm curious if this can be implemented in a way that it's able to coexist with the arithmetic division operator `/`.
+ `src/external/ruby/table.rb`: query results should convert to a `Record` instance instead of raw data.
+ `src/external/ruby/table.rb`: `#insert` should maybe return `self` or a hash of the inserted row instead of just the id.
+ `ore/database.ore`: `connection` member is typed as raw `Sequel::SQLite::Database`, needs a real Ore type.
+ `ore/date_time.ore`: no `Date_Time` class in `scopes.rb` yet.
+ `test/type_checker_test.rb`: no error raised for an identifier with an unknown type on the RHS; maybe should warn.
+ `test/parser_test.rb`: design question of whether `return` should be a prefix keyword instead of producing a `when_true`, and whether `when_true`/`when_false` naming should better convey that they return arrays.
+ Swap directive and comment characters. I want `@ comment` and `#directive`
+ Struct members should be able to say whether a value is required. Found via `learn/structured_types.ore`: `Widget<opts: Options>()` (naming a member with `:` at a *reference* site, not a declaration) doesn't actually set `opts`'s value to `Options`; `w.structure.opts` comes back `nil` there (name registers, type/value slots don't), since a reference site's own `name: Type` reuses the declaration-annotation grammar. `Widget<opts := Options>()` (`:=` instead of `:`) does what the old comment claimed `:` would do. Not sure yet whether the fix is making a bare member optional-by-default (`<opts: Options?>` to opt in) or required-by-default (`<opts: Options!>` to opt out); undecided, revisit.
- Add extra period in the middle of range operators .. -> ...
- Rename @use to @load? Or something else because "use" doesn't clearly explain the behavior.
- Change set comparison operators to `=== =!= =/= =>= =<=` from `=== !== =/= >== ==<` to be more consistent
- Function param declarations should use `:=` to be consistent with the remainder of the declaration syntax
- Func signature literal syntax changed to match real functions: `Return_Type{Params;}` → `{Params -> Return_Type;}`. The old return-type-before-brace prefix (`name: Type { }`) is retired entirely, including for self-declaring signatures.
- Renamed `Member` → `Field` throughout (Ore: `ore/field.ore`, Ruby: `field.rb`/`Ore::Field`); `Shape` (later renamed to `Struct`) kept its own name at the time. Renamed all "slot" terminology to "field" across the codebase.
- Renamed Ruby-only `tag_types` reader to `type_objects` (exists to dodge shadowing inherited `Type#types`; name now says what it returns).
- Retired `identifier: Type { ... }` syntax is now just unparseable as a function, not specially rejected.
- Strict field creation: `.` can no longer fabricate an undeclared field (`Number.yolo = 123` used to "succeed" then be unreadable; `Number.yolo := 123` silently hit the wrong scope). `./`/`../` self-declaration from inside a type's own method is still legit, but only while under construction. One shared impl (`#assign_dot_field`) for all `.`-writes.
- Renamed `Shape` → `Struct` throughout (`shape.rb`/`Ore::Shape` → `struct.rb`/`Ore::Struct`; `ore/shape.ore` → `ore/struct.ore`), and "shaped type"/"tagged type" → "structured type" everywhere. The Ore-level accessor moved from `.shape` to `.structure`.
- Renamed `Field` → `Member` throughout (`field.rb`/`Ore::Field` → `member.rb`/`Ore::Member`; `ore/field.ore` → `ore/member.ore`): "struct member" reads better than "struct field". `.fields` → `.members`.
- Replace `@start` with `@server_start`, and `@shut_down` with `@server_stop`
- Renamed `@cd`/`@cd ..` to `@push_scope`/`@pop_scope`. Separately, replaced the old `@ += instance`/`@ -= instance`/`@param` "sibling scope" mechanism with two distinct mechanisms: `@add_readable_scope`/`@add_writable_scope`/`@remove_readable_scope`/`@remove_writable_scope` for manual control anywhere, plus `@readable`/`@writable` short-form param prefixes for function signatures specifically. `readable_scopes`/`writable_scopes` are `ObjectSpace::WeakMap`s, not `Set`s, so membership never keeps an instance alive on its own. Lookup/write order is `[self, writable, readable]`, most-recently-added-first on a name collision.
- Preload declarations now go into Global's readable scope (`Standard_Library`), not Global's own declarations.
